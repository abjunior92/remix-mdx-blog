---
title: My Second Post
published: 03-24-2024
description: "Lorem ipsum dolor sit, amet consectetur adipisicing elit. Ex fugit suscipit illum excepturi repellat vel, itaque nostrum debitis inventore veritatis recusandae, animi distinctio at voluptates ratione consequuntur minus voluptate eum!"
author: Micheal Scott
---

export const meta = () => {
  return [
    { title: frontmatter.title, published: frontmatter.published },
    {
      name: "description",
      content: frontmatter.description,
    },
  ];
};

## {frontmatter.title}

import DateString from "../components/DateString"
import AuthorString from "../components/AuthorString"

<div style={{
  display: "flex",
  flexDirection: "column"
}}>
<DateString dateString={frontmatter.published} />
<AuthorString author={frontmatter.author} />
</div>

---

#### Lorem ipsum dolor sit amet consectetur adipisicing elit. Neque hic incidunt quaerat quis suscipit totam molestias illo alias voluptas, reiciendis at eaque quidem unde deleniti ad nobis accusamus! Ratione, consequuntur!

```js
function compare<T>(a: T, b: T): number {
  if (a < b) return -1;
  if (a > b) return 1;
  return 0;
}
```

#### Lorem ipsum dolor sit amet consectetur adipisicing elit. Ullam facilis accusantium sed dolor nemo vero omnis provident doloribus earum neque error, magni ab, totam qui, debitis soluta. Officia, magni in!

```js filename="a/hello/file.js"
function sortBy<T>(
  arr: T[],
  key: (item: T) => any,
  dir: "asc" | "desc" = "asc"
) {
  return arr.sort((a, b) => {
    const res = compare(key(a), key(b));
    return dir === "asc" ? res : -res;
  });
}
```

#### Lorem ipsum dolor sit amet consectetur adipisicing elit. Neque hic incidunt quaerat quis suscipit totam molestias illo alias voluptas, reiciendis at eaque quidem unde deleniti ad nobis accusamus! Ratione, consequuntur!

```js
function compare<T>(a: T, b: T): number {
  if (a < b) return -1;
  if (a > b) return 1;
  return 0;
}
```

#### Lorem ipsum dolor sit amet consectetur adipisicing elit. Ullam facilis accusantium sed dolor nemo vero omnis provident doloribus earum neque error, magni ab, totam qui, debitis soluta. Officia, magni in!

```js
function sortBy<T>(
  arr: T[],
  key: (item: T) => any,
  dir: "asc" | "desc" = "asc"
) {
  return arr.sort((a, b) => {
    const res = compare(key(a), key(b));
    return dir === "asc" ? res : -res;
  });
}
```
